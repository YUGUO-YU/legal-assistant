server:
  port: 8080

spring:
  application:
    name: legal-assistant

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/legal_assistant?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: main0126

  data:
    redis:
      host: localhost
      port: 6379
      password: ${REDIS_PASSWORD:}
      database: 0

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  type-aliases-package: com.legal.assistant.module.*.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: assign_uuid
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

jwt:
  secret: ${JWT_SECRET:your-super-secret-key-change-in-production}
  expiration: 604800000
  refresh-expiration: 2592000000

wechat:
  miniapp:
    appid: ${WECHAT_MINIAPP_APPID:wx5f819bd85fd7c4d8}
    secret: ${WECHAT_MINIAPP_SECRET:61a1ab391188e9b928d34735d3421dbb}
  oa:
    appid: ${WECHAT_OA_APPID:}
    secret: ${WECHAT_OA_SECRET:}
    token: ${WECHAT_OA_TOKEN:}
    aes-key: ${WECHAT_OA_AESKEY:}
  open:
    appid: ${WECHAT_OPEN_APPID:}
    secret: ${WECHAT_OPEN_SECRET:}
    redirect-uri: ${WECHAT_OPEN_REDIRECT_URI:http://localhost:5173/wechat/callback}

sms:
  secret-id: ${SMS_SECRET_ID:}
  secret-key: ${SMS_SECRET_KEY:}
  app-id: ${SMS_APP_ID:}
  template-login: ${SMS_TEMPLATE_LOGIN:your-template-id}

cos:
  secret-id: ${COS_SECRET_ID:}
  secret-key: ${COS_SECRET_KEY:}
  bucket: ${COS_BUCKET:}
  region: ${COS_REGION:ap-guangzhou}

third-party:
  api-key: ${THIRD_PARTY_API_KEY:}

logging:
  level:
    com.legal.assistant: debug